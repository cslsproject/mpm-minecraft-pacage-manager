#include <stdio.h>

int main(int argc, char **argv)
{
    int i=1;
    char *command = argv[1];
    char *filename = argv[2];
    if (strcmp(command, "install") == 0)
    {
      
    }
    return 0;
}


#include <stdio.h>
#include <curl/curl.h>

int main(int argc, char **argv)
{
    int i=1;
    char *command = argv[1];
    char *filename = argv[2];
    if (strcmp(command, "install") == 0)
    {
      printf("%s 확인\n", command);  //debug 1 command
      printf("%s 확인\n", filename);  //debug 2 filename
      int main()
      {
        int config/pud/filename.taz.gz=2;
        
        
        
      }
    }
    return 0;
}
